

mcp_servers:
  gmail:
    url: "https://gmail.srv1694637.hstgr.cloud/mcp"
    auth: oauth
    timeout: 120
    connect_timeout: 30
    supports_parallel_tool_calls: false
    tools:
      include: [draft_email, read_email, search_emails, list_email_labels, create_label, download_attachment]
      resources: true
      prompts: false
    enabled: true
